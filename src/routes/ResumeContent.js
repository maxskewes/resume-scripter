import { Box } from '@mui/material';
import React from 'react';
import ContentHeader from '../components/ContentHeader';

const ResumeContent = () => {
  return (
    <Box>
      <ContentHeader percentCompleted='null' />
      ResumeContent
    </Box>
  );
};

export default ResumeContent;
